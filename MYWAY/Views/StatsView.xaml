<UserControl x:Class="MYWAY.Views.StatsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Big Total Points Widget -->
        <Border Grid.Row="0" Grid.ColumnSpan="2" Style="{StaticResource CardStyle}">
            <StackPanel HorizontalAlignment="Center" Margin="20">
                <TextBlock Text="Wszystkie Zdobyte Punkty Dziś" FontSize="20" Foreground="#64748B" HorizontalAlignment="Center"/>
                <TextBlock Text="{Binding TotalPoints}" FontSize="72" FontWeight="Bold" Foreground="#3B82F6" HorizontalAlignment="Center" Margin="0,10,0,0"/>
            </StackPanel>
        </Border>

        <!-- Task Stats Widget -->
        <Border Grid.Row="1" Grid.Column="0" Style="{StaticResource CardStyle}">
            <StackPanel HorizontalAlignment="Center" Margin="10">
                <TextBlock Text="Ukończone Zadania" FontSize="18" Foreground="#64748B" HorizontalAlignment="Center"/>
                <TextBlock Text="{Binding CompletedTasksCount}" FontSize="48" FontWeight="Bold" Foreground="#10B981" HorizontalAlignment="Center" Margin="0,10,0,0"/>
            </StackPanel>
        </Border>

        <!-- Extra Tasks Widget -->
        <Border Grid.Row="1" Grid.Column="1" Style="{StaticResource CardStyle}">
            <StackPanel HorizontalAlignment="Center" Margin="10">
                <TextBlock Text="Ilość Dodatkowych Aktywności" FontSize="18" Foreground="#64748B" HorizontalAlignment="Center"/>
                <TextBlock Text="{Binding ExtraActivities.Count}" FontSize="48" FontWeight="Bold" Foreground="#8B5CF6" HorizontalAlignment="Center" Margin="0,10,0,0"/>
            </StackPanel>
        </Border>

        <!-- Future Extensibility Widget -->
        <Border Grid.Row="2" Grid.ColumnSpan="2" Style="{StaticResource CardStyle}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="Świetna robota!" FontSize="24" FontWeight="Bold" Foreground="#1E293B" HorizontalAlignment="Center"/>
                <TextBlock Text="Utrzymaj swój Streak i wracaj codziennie, by realizować swoje cele." FontSize="16" Foreground="#64748B" HorizontalAlignment="Center" Margin="0,10,0,0" TextWrapping="Wrap"/>
                <!-- You can generate PDF export button here as requested in initial prompt -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                    <Button Content="Wróć do Zadań" Style="{StaticResource SecondaryButton}" Margin="0,0,10,0" Width="200"
                            Command="{Binding ChangeViewCommand}" CommandParameter="0"/>
                    <Button Content="Pobierz Raport PDF (Prototyp)" Style="{StaticResource PrimaryButton}" Background="#475569" Width="250"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
    </ScrollViewer>
</UserControl>
