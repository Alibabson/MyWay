<UserControl x:Class="MYWAY.Views.HabitsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top Header: Quote and Mood -->
        <Border Grid.Row="0" Style="{StaticResource CardStyle}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="10,0,0,0">
                    <TextBlock Text="Cytat dnia:" FontSize="14" Foreground="#94A3B8" FontStyle="Italic"/>
                    <TextBlock Text="{Binding TodayRecord.QuoteOfTheDay}" FontSize="18" FontWeight="SemiBold" Foreground="#475569" TextWrapping="Wrap" MaxWidth="600" Margin="0,5,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" VerticalAlignment="Center" Orientation="Horizontal" Margin="0,0,20,0">
                    <TextBlock Text="Samopoczucie (1-5): " VerticalAlignment="Center" FontSize="16" FontWeight="SemiBold" Margin="0,0,10,0" Foreground="#334155"/>
                    <TextBox Text="{Binding TodayRecord.MoodScore, UpdateSourceTrigger=PropertyChanged}" Width="50" Height="40" TextAlignment="Center" VerticalContentAlignment="Center" FontSize="20" FontWeight="Bold" Background="#F1F5F9" BorderThickness="0"/>
                </StackPanel>
                
                <Button Grid.Column="2" Content="Wróć do Zadań" Style="{StaticResource SecondaryButton}" VerticalAlignment="Center" Margin="0,0,10,0"
                        Command="{Binding ChangeViewCommand}" CommandParameter="0"/>
            </Grid>
        </Border>

        <!-- Main Content: Habits List -->
        <Border Grid.Row="1" Style="{StaticResource CardStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Nawyki (Streaki)" FontSize="22" FontWeight="Bold" Foreground="#1E293B" Margin="0,0,0,15"/>

                <!-- Add habit form -->
                <Grid Grid.Row="1" Margin="0,0,0,15">
                     <Grid.ColumnDefinitions>
                         <ColumnDefinition Width="*"/>
                         <ColumnDefinition Width="Auto"/>
                     </Grid.ColumnDefinitions>
                     <TextBox Grid.Column="0" Text="{Binding NewHabitTitle, UpdateSourceTrigger=PropertyChanged}" Padding="10,8" FontSize="14" Margin="0,0,10,0" ToolTip="Nazwa nowego nawyku..."/>
                     <Button Grid.Column="1" Content="Dodaj Nawyk" Style="{StaticResource PrimaryButton}" Command="{Binding AddHabitCommand}"/>
                </Grid>

                <ListBox Grid.Row="2" ItemsSource="{Binding Habits}" BorderThickness="0" Background="Transparent" HorizontalContentAlignment="Stretch">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#F8FAFC" CornerRadius="8" Padding="20" Margin="0,0,0,15">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Title}" FontSize="18" FontWeight="SemiBold" Foreground="#334155"/>
                                        <TextBlock Text="{Binding CurrentStreak, StringFormat='🔥 Aktualny Streak: {0} dni (Rekord: {0})'}" FontSize="14" Foreground="#F59E0B" FontWeight="Bold" Margin="0,5,0,0"/>
                                    </StackPanel>

                                    <!-- Habit Status Indicator -->
                                    <TextBlock Grid.Column="1" Text="✅ Wykonane dziś" Visibility="{Binding IsCompletedToday, Converter={StaticResource BooleanToVisibilityConverter}}" Foreground="#10B981" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,20,0" FontSize="16"/>

                                    <Button Grid.Column="2" Content="Zalicz Nawyk" Background="#10B981" Foreground="White" BorderThickness="0" Padding="15,10"
                                            Command="{Binding DataContext.CompleteHabitCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" VerticalAlignment="Center" 
                                            Visibility="{Binding IsCompletedToday, Converter={StaticResource InvertedBooleanToVisibilityConverter}}">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" CornerRadius="6">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Button.Template>
                                    </Button>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>
    </Grid>
</UserControl>
